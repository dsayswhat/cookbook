<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title }}</title>
    <link rel="stylesheet" href="{{ '/css/styles.css' | url }}">
</head>
<body class="min-h-screen bg-gray-50">
    <header class="bg-primary-800 text-white">
        <div class="container-recipe py-4">
            <h1 class="text-2xl font-display font-bold">
                <a href="{{ '/' | url }}" class="hover:underline">Recipe Cookbook</a>
            </h1>
            <nav class="mt-2">
                <ul class="flex space-x-4">
                    <li><a href="{{ '/' | url }}" class="hover:underline">Home</a></li>
                </ul>
            </nav>
        </div>
    </header>
    <main class="container-recipe py-8">
        {{ content | safe }}
    </main>
    <footer class="bg-accent-50 border-t border-accent-100 py-4 mt-8">
        <div class="container-recipe text-center text-gray-600">
            <p>&copy; 2025 Recipe Cookbook</p>
        </div>
    </footer>
</body>
</html> 